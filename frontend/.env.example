EXPO_PUBLIC_API_URL=http://localhost:3000
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Optional: https link to install the app, put in shared and emailed invites. Defaults to the Loadly page
# (https://loadly.io/kzM0n7qg); set it when the app moves to Google Play. Restart Metro with --clear after changing it.
EXPO_PUBLIC_APP_DOWNLOAD_URL=
LOADLY_API_KEY=
LOADLY_USER_KEY=
